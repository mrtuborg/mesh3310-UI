# Pinned to the CI image that ships Zephyr SDK 0.16.x, matching west.yml (Zephyr 3.6.0).
# Do not use :latest — it now ships SDK 1.0.1 which is incompatible with Zephyr 3.6.0.
FROM zephyrprojectrtos/ci:v0.26.14
WORKDIR /work

